import { Box } from "@chakra-ui/react";
import React from "react";

type Props = {};

const GenerationTab: React.FunctionComponent<Props> = (props) => {
  return <Box>Generation tab content</Box>;
};

export default GenerationTab;
